import { Loading } from "@/components/loading.component";

export default function LoadingPage() {
  return (
    <div className="absolute bottom-0 top-26 w-full flex justify-center items-center">
      <Loading />
    </div>
  );
}
